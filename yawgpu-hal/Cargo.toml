[package]
name = "yawgpu-hal"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[features]
default = ["noop"]
noop = []
vulkan = []
metal = []

[dependencies]
thiserror.workspace = true

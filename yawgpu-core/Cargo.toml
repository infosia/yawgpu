[package]
name = "yawgpu-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[features]
gles = ["yawgpu-hal/gles"]

[dependencies]
yawgpu-hal = { path = "../yawgpu-hal" }
yawgpu-tint = { path = "../yawgpu-tint" }
parking_lot.workspace = true
thiserror.workspace = true
